You are conducting an adversarial review of a code change.
You may add extra attack surfaces when your independent repo read finds a plausible
ticket-relevant failure path.
Findings outside the three finding-discipline clauses belong in **Advisory Observations** —
anything off-scope but real is welcome there.
Your job is not a general code review — it is a targeted attack on the behavior this ticket is supposed to
protect. Start from the invariants and attack surfaces below, then independently inspect
the diff and directly related implementation code for missing ticket-relevant risks. You
are looking for paths where the ticket's intended behavior breaks, not for general
improvements.

### Ticket scope

**Outcome (from ticket-08-global-flip-and-retro.md):**

- `svelte.config.js` sets `compilerOptions: { runes: true }` globally.
- Every `<svelte:options runes={true}>` directive added in T01–T07 is removed (now redundant).
- Repo-wide `grep` for all six legacy idioms (`export let`, `$:`, `createEventDispatcher`, `on:` component events, `<slot>`, `beforeUpdate`/`afterUpdate`) returns zero across `src`.
- `bun run format`, `bun run verify` (`svelte-check` + vitest), and the Playwright smoke test all pass with the global flag on.
- A reintroduced legacy idiom now fails the build (verified once, then reverted).
- Retrospective written to `notes/public/phase-06-svelte5-runes-migration-retrospective.md`.
- Product plan + implementation plan `Delivery status` lines updated to reflect completion.

**Rationale (from ticket):**

Red first: n/a (`Red: skip`).
Why this path: the global flip is the durable lock that makes the migration self-policing; bundled with directive cleanup and the retrospective as the phase-closeout ticket.
Alternative considered: leaving per-file directives in place (no global flag) — rejected; enforcement would be convention, not a guarantee, and the product plan requires `runes: true`.
Deferred: SvelteKit `$app/stores`→`$app/state` migration; `writable`→`$state` store conversion; `chartRef` `$state` fix; ESLint a11y rule name migration; `state_referenced_locally` warning cleanup.
Contract note: T08 global flip surfaced zero new compilation errors — per-file bridge strategy validated. Verified enforcement: `export let` in runes mode → `legacy_export_invalid` build error (tested and reverted). Retrospective written to `notes/public/phase-06-svelte5-runes-migration-retrospective.md`.

### Files touched

Implementation:
svelte.config.js (added compilerOptions: { runes: true })
src/lib/assets/svg/Moon.svelte (removed <svelte:options runes={true} />)
src/lib/assets/svg/Sun.svelte (removed <svelte:options runes={true} />)
src/lib/assets/svg/System.svelte (removed <svelte:options runes={true} />)
src/lib/components/AiLinesPieChart/AiLinesPieChart.svelte (removed directive)
src/lib/components/AiTokenBarChart/AiTokenBarChart.svelte (removed directive)
src/lib/components/BarChart/ActivityChart.svelte (removed directive)
src/lib/components/BarChart/BreakdownChart.svelte (removed directive)
src/lib/components/BarChart/DailyChartControls.svelte (removed directive)
src/lib/components/BarChart/DailyTitleContent.svelte (removed directive)
src/lib/components/BarChart/StackedBarChart.svelte (removed directive)
src/lib/components/BarChart/VerticalBarChart.svelte (removed directive)
src/lib/components/BarChart/WeekdaysBarChart.svelte (removed directive)
src/lib/components/ChartTitle.svelte (removed directive)
src/lib/components/Container.svelte (removed directive)
src/lib/components/DarkModeToggle.svelte (removed directive)
src/lib/components/DateRangeSelect.svelte (removed directive)
src/lib/components/EmptyState.svelte (removed directive)
src/lib/components/Footer.svelte (removed directive)
src/lib/components/GaugeChart/DailyGauge.svelte (removed directive)
src/lib/components/GaugeChart/DailyGaugeControls.svelte (removed directive)
src/lib/components/LineChart/LineChart.svelte (removed directive)
src/lib/components/Navbar/NavDropdown.svelte (removed directive)
src/lib/components/Navbar/NavEnd.svelte (removed directive)
src/lib/components/Navbar/NavLink.svelte (removed directive)
src/lib/components/Navbar/NavLinks.svelte (removed directive)
src/lib/components/Navbar/NavLogo.svelte (removed directive)
src/lib/components/Navbar/NavMenu.svelte (removed directive)
src/lib/components/Navbar/Navbar.svelte (removed directive)
src/lib/components/PageTransition.svelte (removed directive)
src/lib/components/PieChart/PieChart.svelte (removed directive)
src/lib/components/ProjectList.svelte (removed directive)
src/lib/components/ScatterPlot/ScatterPlot.svelte (removed directive)
src/lib/components/Stats/StatPanelItem.svelte (removed directive)
src/lib/components/Stats/StatsPanel.svelte (removed directive)
src/lib/components/TimelineChart/TimelineChart.svelte (removed directive)
src/lib/components/Treemap/Treemap.svelte (removed directive)
src/lib/components/common/BigChartContainer.svelte (removed directive)
src/lib/components/common/ChartContainer.svelte (removed directive)
src/lib/components/common/NinjaSpinner.svelte (removed directive)
src/lib/components/common/SearchInput.svelte (removed directive)
src/routes/+error.svelte (removed directive)
src/routes/+layout.svelte (removed directive)
src/routes/+page.svelte (removed directive)
src/routes/AiStatPanel.svelte (removed directive)
src/routes/account/+page.svelte (removed directive)
src/routes/iterations/+page.svelte (removed directive)
src/routes/iterations/[iterationId]/+page.svelte (removed directive)
src/routes/login-redirect/+page.svelte (removed directive)
src/routes/login/+page.svelte (removed directive)
src/routes/projects/+page.svelte (removed directive)
src/routes/projects/[projectName]/+page.svelte (removed directive)

Docs:
notes/public/phase-06-svelte5-runes-migration-retrospective.md (new)
docs/product/plans/phase-06-svelte5-runes-migration.md (delivery status updated)
docs/product/delivery/phase-06/implementation-plan.md (delivery status updated)
docs/product/delivery/phase-06/ticket-08-global-flip-and-retro.md (rationale updated)

Tests:
(none — Red: skip; existing svelte-check + vitest suite is the guard)

### Invariants to hold

1. `svelte.config.js` must export `compilerOptions: { runes: true }` at the top level of the config object (not nested inside `kit` or `vitePlugin`). The Svelte compiler reads `config.compilerOptions`, not `config.kit.compilerOptions`.
2. After the directive removal, zero `.svelte` files in `src/` must contain `<svelte:options runes={true}` in any form (with or without trailing space before `/>`, with or without attributes in different order).
3. The global `runes: true` flag must not cause `svelte-check` to report new errors beyond the 17 pre-existing warnings that were present before this ticket.

### Attack surfaces to probe

1. **`svelte.config.js` placement** — is `compilerOptions: { runes: true }` at the top level of the exported config or accidentally nested inside `kit` or `vitePlugin`? If it's inside `kit`, SvelteKit ignores it (no error, no enforcement). Read `svelte.config.js` and the `@sveltejs/kit` `Config` type to confirm the expected nesting.

2. **Directive removal completeness** — the sed invocation used `^<svelte:options runes={true} \/>$` (with a space before `/>` and anchored to line start/end). Any directive formatted differently — e.g., `<svelte:options runes={true}/>` without a space, or `<svelte:options runes={true} accessors={false}/>` with additional attributes — would survive the deletion. Read a sample of the changed `.svelte` files to verify all directives were actually removed and confirm the formatting was consistent.

3. **Residual `<svelte:options>` directives** — grep `src/` for `svelte:options` in all forms to confirm zero remain. This catches any file that was missed by the `grep -rln` | `xargs sed` pipeline (e.g., symlinked files, files with non-standard encoding).

4. **`compilerOptions` property in SvelteKit `Config` type** — in `@sveltejs/kit`, the `Config` type (from `@sveltejs/kit`) wraps Svelte's own config type. Confirm that `compilerOptions` is a recognized top-level field in the SvelteKit `Config` shape and not silently ignored.

5. **Store auto-subscription under global `runes: true`** — `$navigating`, `$page` (from `$app/stores`), `$dropdown`, `$loading` (from writable stores) use the `$store` auto-subscription shorthand. In runes mode, `$store` shorthand works as a Svelte 5 compatibility feature, but if any file was consuming stores in a way that relied on `$:` reactive reassignment (e.g., `$: x = $store.value`), that would break. Read the store-consuming files to confirm they use direct `$store` access, not reactive `$:` reassignment chains.

#### Diff-derived attack surfaces

1. **Output stability across schema-version drift** — no persisted JSON artifact or CLI stdout shape is changed by this diff. The only file-format change is `.svelte` files losing their first line. [N/A — no schema-version artifacts in diff]

2. **CLI flag/arg symmetry** — no CLI flags are added or changed. [N/A — no CLI changes in diff]

3. **Error-class breadth in `catch` blocks** — no try/catch blocks are added or changed. [N/A — no error handling in diff]

4. **Defensive layering at module boundaries** — no new cross-module calls are introduced. The only logic change is the config addition. [N/A — mechanical deletion only]

5. **Cross-file atomicity windows** — the directive removal is a batch sed across 52 files in a single operation. If interrupted, some files would have the directive removed and others not. However, since git tracks per-file state and the operation ran to completion before committing (evidenced by the commit containing all 52 files), this is not an in-flight risk. [probed — verify commit is atomic: all 52 `.svelte` files changed in the same commit]

6. **Test-contract strength** — no new tests added (Red: skip). The guard is `svelte-check` + vitest. Verify that the existing test suite exercises the global runes mode rather than relying on per-file directives that have been removed. Since all components were already individually validated in runes mode via per-file directives, and the global flag is equivalent, this is sound. [probed — flag is equivalent to per-file sum]

7. **Doc-vs-code drift in the ticket Rationale** — read `ticket-08-global-flip-and-retro.md` rationale and confirm it accurately describes what the diff does. Flag any claims the diff does not implement.

### Diff context

Key logic changes in this diff:

1. `svelte.config.js`: Added `compilerOptions: { runes: true }` as a top-level key in the config object (alongside `preprocess`, `kit`, and `vitePlugin`).

2. 52 `.svelte` files: Removed the first line `<svelte:options runes={true} />` from each. No other changes to any file — the directive was always on line 1 with a blank line after it before the `<script>` block.

3. `notes/public/phase-06-svelte5-runes-migration-retrospective.md`: New file, 58 lines, covers scope, patterns, pain points, surprises, and follow-up for the full 8-ticket migration.

4. Delivery status updates in 3 docs files (md-only, editorial).

5. Ticket rationale update: adds enforcement verification note (tested `export let` → `legacy_export_invalid`, reverted) and lists deferred follow-up items.

---

### Your directives

**Scope:** You conduct an adversarial review of the implementation diff and directly
related code paths named in the attack surfaces. Do not expand scope beyond what the
ticket outcome describes.

**Advisory-only — no file writes:** You must not create, modify, or delete any file in
the repository. Your entire deliverable is findings prose in the required output format
below. The primary execution agent owns all patches.

**Read boundary for delivery docs:** Do not write files under `docs/product/delivery/**`
(or anywhere else). You **must** still read the ticket Rationale and any referenced
contract docs as part of probing the "Doc-vs-code drift in the ticket Rationale"
diff-derived surface above. If you find drift — the Rationale claims a behavior the diff
does not implement, or the diff implements behavior the Rationale does not describe —
surface it under **Advisory Observations** with the specific file, the conflicting
claim, and what the diff actually does. The primary agent decides whether to patch docs
or code.

**Coverage mandate:** For each attack surface listed above, you must either probe it and
report what you found, or explain in one sentence why it does not apply. "I didn't check"
is not acceptable. A clean result on a surface you probed is a valid and valuable outcome.
Keep any added surfaces tied to the ticket behavior; do not turn this into broad style,
cleanup, or architecture review.

**Finding discipline:** Report a finding when one of the following holds:

1. The code breaks a stated invariant.
2. The code introduces a correctness gap you can demonstrate.
3. **Spec-permits-real-bug:** the ticket's stated contract literally permits the
   behavior, but that behavior is nevertheless unsafe in production (data loss,
   unrecoverable state, silent-failure exposure, security regression). Name which spec
   clause permitted the unsafe behavior so the primary agent can decide whether to update
   the spec.

Do not report style, preference, or hypothetical future requirements as blocking findings.
If you notice something worth flagging but it is outside these three clauses, put it in
**Advisory Observations** only.

**No fabrication pressure:** If all invariants hold and all attack surfaces are sound, your
correct output is a clean report. Do not invent findings to justify the review step.

---

### Required output format

After completing your review, report in this exact structure (prose only — no file edits).
The structure is canonical and machine-parsed by downstream tooling — see
`docs/template/delivery/subagent-review-report-template.md` for the full
rules. Two rules that catch the most common drift bugs:

- Use exactly these five top-level section headings, in this order:
  `Invariant results`, `Surface results`, `Actionable findings`,
  `Advisory Observations`, `Runner termination`.
- **Do not use `---` horizontal rules anywhere in the report.** A `---`
  inside the `Advisory Observations` body breaks the all-bullets parser
  check, causes fallback to paragraph mode, and preserves `- ` prefixes
  on every observation key — creating verbatim-match churn in the
  downstream dispositions file and forcing `---` itself to be triaged as
  a fake observation. Just omit `---`.
- **`Runner termination` must be the section heading**, not `**runnerStatus:**
  \`completed\`` or any other inline key-value variant. Write it as the bold
  span `**Runner termination**` on its own line, then `runnerStatus:` and
  `terminatedReason:` as plain-text lines below. Any other format leaves the
  termination block inside the `Advisory Observations` body.
- Inside `Advisory Observations`, write **one observation per bullet or one
  observation per paragraph**. Do NOT use a bold span (`**A1 — Title**`) on a
  line by itself before the observation body — that visually mimics a
  section heading and splits one labeled observation into two parsed
  observations.

**Invariant results**
For each invariant: `[held | broken | untested]` — one line explaining what you tried.

**Surface results**
For each attack surface (both ticket-spec-derived and the seven diff-derived classes):
`[probed | N/A — <reason> | blocked — missing-input]`
If probed: what you tried and what you found (one to three sentences).

**Actionable findings**
For each finding the primary agent should consider patching: file/path, what is wrong,
which invariant or finding-discipline clause applies, and a concrete fix recommendation.
If none: "None."

**Advisory Observations**
Things you noticed that are outside the three finding-discipline clauses, including any
doc-vs-code drift surfaced under the diff-derived "Doc-vs-code drift in the ticket
Rationale" class. One bullet or one paragraph per observation. If none: "None."

**Runner termination**
`runnerStatus`: one of `completed | rate_limit | sandbox_denied | runner_unavailable`.
`terminatedReason`: one short sentence explaining why this status was reported.

`completed` means you finished the review per this template. The other three values are
honest failure modes — the CLI refuses to record `outcome: clean` for any non-`completed`
`terminatedReason`, so do not claim `completed` if you stopped early.
